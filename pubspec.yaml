name: untitled3
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^2.32.0
  google_fonts: ^6.2.1
  animate_do: ^3.3.4
  firebase_auth: ^4.19.0
  fl_chart: ^0.68.0
  image_picker: ^1.1.2
  firebase_storage: ^11.7.0
  firebase_database: ^10.5.5 
  google_sign_in: ^6.2.1
  onesignal_flutter: ^5.1.0
  intl: ^0.19.0
  drag_and_drop_lists: ^0.4.2 # FIX: Updated to a newer, compatible version
  firebase_messaging: ^14.9.4
  flutter_local_notifications: ^19.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
