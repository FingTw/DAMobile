name: apk_qlda
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.10.1


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  onesignal_flutter: ^5.3.4
  http: ^1.1.0
  intl: ^0.20.2

  # --- KHU VỰC FIREBASE (Đừng ghi số version, để nó tự tìm) ---
  firebase_core:
  firebase_database:
dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^6.0.0


flutter:

  uses-material-design: true


